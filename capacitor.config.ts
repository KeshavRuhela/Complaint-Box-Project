import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.complainBox.app',
  appName: 'ComplainBox',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
